import 'package:flutter/material.dart';

class RejectedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("RejectedPage"),
      ),
      body: Container(
        child: Center(child: Text("RejectedPage"),),
      ),
    );
  }
}