import 'package:flutter/material.dart';

class UnderReviewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("UnderReviewPage"),
      ),
      body: Container(
        child: Center(child: Text("UnderReviewPage"),),
      ),
    );
  }
}